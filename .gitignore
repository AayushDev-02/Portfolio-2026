node_modules/
.pnpm-store/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Personal-data source documents. NEVER commit these.
# The 履歴書 in particular carries a home address, phone number, date of
# birth, gender and nationality. Committing one means rewriting history to
# remove it, and it is public the moment it is pushed.
docs/source/

